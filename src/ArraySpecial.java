import javax.sound.midi.Soundbank;
import java.util.Arrays;

public class ArraySpecial {
    public static void main(String[] args) {

        //array remove element

//        int[] array = {2, 4, 6,5,7};
//        int indexToRemove = 2;
//
//        int[] newArray = new int[array.length - 1];
//
//        System.arraycopy(array, 0, newArray, 0, indexToRemove);
//
//        System.arraycopy(array, indexToRemove + 1, newArray, indexToRemove, array.length - indexToRemove - 1);
//
//        System.out.println(Arrays.toString(newArray));








        //array add element

//        int[] originalArray = {1, 2, 3, 4, 5};
//        int newElement = 6;
//
//
//        int[] newArray = Arrays.copyOf(originalArray, originalArray.length + 1);
//
//        newArray[newArray.length - 1] = newElement;
//
//        System.out.println("Original Array: " + Arrays.toString(originalArray));
//        System.out.println("Array adding element: " + Arrays.toString(newArray));


//        int [] array = {1,2,3,4,5};
//
//        int [] newArray = Arrays.copyOf(array,array.length+1);
//
//        newArray[newArray.length-1] =6;
//        System.out.println(Arrays.toString(newArray));










        //sort array

//        int[] array = {6, 2, 4}; // Unsorted array
//
//        // Sort the array
//        Arrays.sort(array);
//
//        System.out.println("Sorted array: " + Arrays.toString(array));
    }
}
